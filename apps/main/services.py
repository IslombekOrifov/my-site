def upload_sertificate_path(instance, image):
    return f"sertificates/{image}"


def upload_coding_lang_path(instance, image):
    return f"coding/logo/{image}"


def upload_quote_path(instance, image):
    return f"quote/{image}"


def upload_cv_path(instance, file):
    return f"cv/{file}"

def upload_avatar_path(instance, image):
    return f"avatar/{image}"

def upload_client_logo_path(instance, logo):
    return f"clients/{logo}"